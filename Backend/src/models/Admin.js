// Admin model removed — using MySQL `admins` table via Backend/src/config/database.js pool.
// If you need a JS model abstraction, re-add here. Kept as placeholder file to avoid import errors.
export default null;
